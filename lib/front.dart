import 'package:flutter/material.dart';

class xapp extends StatelessWidget {
  const xapp({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("decte"),),
    body: Column(
    children:[

        ]

    ));
  }
}
